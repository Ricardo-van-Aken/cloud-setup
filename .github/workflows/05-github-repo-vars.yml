name: foundation - 05-github-repo-vars

on:
  workflow_dispatch:
    inputs:
      action:
        description: "Terraform action"
        required: true
        default: "plan-apply"
        type: choice
        options:
          - plan-apply
          - plan-destroy
          - apply
          - destroy

env:
  AWS_ACCESS_KEY_ID: ${{ vars.DO_STATE_BUCKET_ACCESS_KEY }}
  TF_VAR_region: ${{ vars.DO_STATE_BUCKET_REGION }}
  TF_VAR_bucket_name: ${{ vars.DO_STATE_BUCKET_NAME }}
  TF_VAR_github_organization: ${{ vars._GITHUB_ORGANIZATION_NAME }}

jobs:
  validate:
    name: Validate branch
    runs-on: ubuntu-latest
    steps:
      - name: Require production branch for apply/destroy
        if: inputs.action == 'apply' || inputs.action == 'destroy'
        shell: bash
        run: |
          if [ "${{ github.ref_name }}" != "production" ]; then
            echo "::error::apply and destroy must be triggered from the production branch (current: ${{ github.ref_name }})"
            exit 1
          fi

  plan:
    name: Tofu (05-github-repo-vars) - Plan
    runs-on: ubuntu-latest
    needs: validate
    env:
      AWS_SECRET_ACCESS_KEY: ${{ secrets.DO_STATE_BUCKET_SECRET_KEY }}
      TF_VAR_do_org_infra_token: ${{ secrets.DO_ORG_INFRA_TOKEN }}
      TF_VAR_github_org_token: ${{ secrets._GITHUB_ORG_TOKEN }}
      TF_VAR_github_repo_token: ${{ secrets._GITHUB_REPO_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - uses: ./.github/actions/tofu-plan
        with:
          working_dir: /workspace/foundation/terraform/05-github-repo-vars
          state_key: foundation/github-repo-vars/terraform.tfstate
          action: ${{ inputs.action }}
          artifact_name: terraform-plan-05-github-repo-vars

  apply-approved-plan:
    name: Tofu (05-github-repo-vars) - Apply (after manual approval)
    if: inputs.action == 'apply' || inputs.action == 'destroy'
    runs-on: ubuntu-latest
    needs: [validate, plan]
    environment: production
    env:
      AWS_SECRET_ACCESS_KEY: ${{ secrets.DO_STATE_BUCKET_SECRET_KEY }}
      TF_VAR_do_org_infra_token: ${{ secrets.DO_ORG_INFRA_TOKEN }}
      TF_VAR_github_org_token: ${{ secrets._GITHUB_ORG_TOKEN }}
      TF_VAR_github_repo_token: ${{ secrets._GITHUB_REPO_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - uses: ./.github/actions/tofu-apply
        with:
          working_dir: /workspace/foundation/terraform/05-github-repo-vars
          state_key: foundation/github-repo-vars/terraform.tfstate
          artifact_name: terraform-plan-05-github-repo-vars
