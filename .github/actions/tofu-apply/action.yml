name: tofu-apply
description: Download a plan artifact and apply it with OpenTofu in a Docker container. Set all TF_VAR_* and AWS_* env vars in the calling job.

inputs:
  working_dir:
    description: "Terraform working directory inside the container"
    required: true
  state_key:
    description: "Terraform remote state key"
    required: true
  artifact_name:
    description: "Name of the plan artifact to download"
    required: true

runs:
  using: composite
  steps:
    - uses: actions/checkout@v4
      with:
        ref: production
        submodules: true

    - name: Build image
      shell: bash
      run: docker build -t cloud-setup-tofu $GITHUB_WORKSPACE

    - name: Download plan artifact
      uses: actions/download-artifact@v4
      with:
        name: ${{ inputs.artifact_name }}

    - name: Write env file
      shell: bash
      run: printenv | grep -E '^(AWS_ACCESS_KEY_ID=|AWS_SECRET_ACCESS_KEY=|TF_VAR_)' | grep -v '=$' > /tmp/tofu.env

    - name: Start container
      shell: bash
      run: |
        docker run -d --name cloud-setup-tofu-${{ github.run_id }} \
          -w ${{ inputs.working_dir }} \
          --env-file /tmp/tofu.env \
          cloud-setup-tofu sleep infinity

    - name: Generate Backend Configuration
      shell: bash
      run: |
        docker exec cloud-setup-tofu-${{ github.run_id }} bash -c "
          source ../../scripts/common.sh
          generate_backend_file \"\$TF_VAR_region\" \"\$TF_VAR_bucket_name\" '../backend.hcl'
        "

    - name: Tofu Init
      shell: bash
      run: |
        docker exec cloud-setup-tofu-${{ github.run_id }} \
          tofu init -backend-config ../backend.hcl \
                    -backend-config="key=${{ inputs.state_key }}"

    - name: Inject plan into container
      shell: bash
      run: docker cp tfplan cloud-setup-tofu-${{ github.run_id }}:${{ inputs.working_dir }}/tfplan

    - name: Tofu Apply
      shell: bash
      run: docker exec cloud-setup-tofu-${{ github.run_id }} tofu apply tfplan

    - name: Stop container
      if: always()
      shell: bash
      run: |
        docker rm -f cloud-setup-tofu-${{ github.run_id }}
        rm -f /tmp/tofu.env
